version: 2
project_name: slack-tui

before:
  hooks:
    - go mod tidy

builds:
  - main: .
    binary: slack-tui
    env:
      - CGO_ENABLED=0
    goos: [darwin, linux, windows]
    goarch: [amd64, arm64]
    ignore:
      - goos: windows
        goarch: arm64
    # SLACK_APP_CLIENT_ID stamps the published app's client ID so a plain install
    # can sign in with nothing configured. It is NOT a secret — the shipped flow
    # is PKCE, which is precisely the OAuth mode for clients that cannot keep
    # one. Unset (forks, local `goreleaser release --snapshot`) it stays empty
    # and those builds fall back to a user-supplied app in oauth.json.
    ldflags:
      - -s -w -X main.version={{.Version}} -X github.com/kurenn/slack-tui/internal/config.DefaultClientID={{ envOrDefault "SLACK_APP_CLIENT_ID" "" }}

archives:
  - formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]
    files:
      - README.md
      - LICENSE
      - slack-app-manifest.yaml
      - slack-app-manifest.json

checksum:
  name_template: checksums.txt

changelog:
  use: git
  sort: asc
  groups:
    - title: Features
      regexp: '^.*?feat(\(.+\))??!?:.+$'
      order: 0
    - title: Fixes
      regexp: '^.*?fix(\(.+\))??!?:.+$'
      order: 1
    - title: Other
      order: 999

release:
  draft: false
  prerelease: auto

# Publishes a formula to kurenn/homebrew-tap on each release. Deliberately a
# formula and NOT the newer homebrew_casks: casks run Gatekeeper on install,
# which blocks our unsigned (not notarized) binaries — formulae install
# without quarantine, the standard path for Go CLIs. Needs the
# HOMEBREW_TAP_TOKEN repo secret (a fine-grained PAT with contents:write on
# the tap repo) — the default GITHUB_TOKEN can't push across repos.
brews:
  - repository:
      owner: kurenn
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    directory: Formula
    homepage: https://github.com/kurenn/slack-tui
    description: A keyboard-first terminal Slack client
    license: MIT
    test: |
      system "#{bin}/slack-tui", "--version"
